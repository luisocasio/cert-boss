// import API from Amplify library
import { API } from "aws-amplify";
import Nav from "./components/Nav";

const App = () => {
  return (
    <div className="App">
      <Nav />
    </div>
  );
};

export default App;
